@model IEnumerable<bookyourdoctor.admin>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Dasbord.cshtml";
}

<h2 class=" head" style="text-align:center"> <u> <b>Admin</b> </u> </h2>

<p>
    @Html.ActionLink("Create", "Create","doctors")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.email }) |
            @Html.ActionLink("Details", "Details", new { id=item.email }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.email })
        </td>
    </tr>
}

</table>
